{% extends 'base.html.twig' %}

{% block body %}
    <div class="container-fluid">
        <div class="row">
            <nav class="col-md-3 col-lg-2 d-none d-md-block bg-dark sidebar min-vh-100">
                <div class="d-flex flex-column justify-content-between h-100">
                    <div class="position-sticky top-0 pt-4">
                        <h1 class="text-white ms-3">Admin</h1>
                        <ul class="nav flex-column">
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('admin_media_index') }}">
                                    Medias
                                </a>
                            </li>
                            {% if is_granted('ROLE_ADMIN') %}
                                <li class="nav-item">
                                    <a class="nav-link" href="{{ path('admin_album_index') }}">
                                        Albums
                                    </a>
                                </li>
                                <li class="nav-item">
                                    <a class="nav-link" href="{{ path('admin_guest_index') }}">
                                        Invités
                                    </a>
                                </li>
                            {% endif %}
                        </ul>
                    </div>
                    <div class="position-sticky bottom-0">
                        <a class="btn btn-primary w-100 mb-4" href="{{ path('home') }}">
                            Retourner au site
                        </a>
                    </div>
                </div>
            </nav>
            <main class="col-md-9 ms-sm-auto col-lg-10 p-4">
                {% block admin %}{% endblock %}
            </main>
        </div>
    </div>
{% endblock %}